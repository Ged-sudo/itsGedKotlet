name: page
on:
  schedule:
    # Runs every hour
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  update-readme-with-blog:
    name: show info
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      
